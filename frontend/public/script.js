/**
 * Babel: Responsável por transpilar o código do React para um código que o browser seja capaz de interpretar. 
 * Vamos utilizar junto com o babel o webpack.
 * 
 * Webpack: Para cada tipo de arquivo, será convertido o código de uma maneira diferente. 
 * Portanto a conversão de código é diferente entre arquivos.
 * Vamos utilizar para isso os Loaders.
 * 
 * Loaders:
 * babel-loader, css-loader, image-loader.
 */